# Tests

`Auraline.Host.Tests` covers M1 configuration bootstrap/load/save/validation, malformed-file preservation, provider transitions/reconnect/refresh/cancellation, retry policy, Windows single-instance signaling, tray resources, Windows path layout and startup-registration adapters, cross-site POST rejection, Resonance Signal v1 integration parsing, health serialization, and contract compatibility.

Rendering, render-session, transport, and functional plugin tests arrive with their implementation milestones.
